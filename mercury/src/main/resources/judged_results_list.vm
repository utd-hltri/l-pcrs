#if( $results.size() > 0 )
<ol class="mdl-list">
	#set( $i = 1 )
	#foreach( $item in $results )
      <li value="$i" class="mdl-list__item">
				#parse("judged_hit.vm")
      </li>
		#set( $i = $i + 1)
	#end
</ol>
#end